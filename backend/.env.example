APP_NAME=develop-backend
CORS_ORIGINS=["http://localhost:5173"]
# 設定すると永続化 (SQLite) が有効になり、再起動してもデータが残る (要件 N-2)。
# 未設定なら in-memory のみ。本番では事前に `alembic upgrade head` を実行する。
# DATABASE_URL=sqlite:///./accounting.db
